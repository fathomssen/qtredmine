sudo: required
dist: trusty

install:
  - sudo add-apt-repository -y ppa:ubuntu-sdk-team/ppa
  - sudo add-apt-repository -y ppa:ubuntu-toolchain-r/test
  - sudo apt-get -qq update
  - sudo apt-get install -y qtbase5-dev gcc-4.9

script:
  - qmake -project
  - qmake qtredmine.pro
  - make
